#include <cstdio>
#include <iostream>

#include <base/color_similarity.hpp>
#include <base/image.hpp>

#include "image_utility.hpp"

int main()
{
    // std::string filepath;
    // std::cin >> filepath;
    // Image img = loadImageFromFile(filepath);
    // printf("Width: %d, Height: %d\n", img.width, img.height);
    // auto rgb = img.at<RgbColor>(0, 0);
    // printf("Pos(0, 0)-color: [%d, %d, %d]", rgb.r, rgb.g, rgb.b);

    Image img(1000, 400);

    return 0;
}
